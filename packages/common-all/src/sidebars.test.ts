// see previous commit (refactor: remove unused `note2TreeDatanote` function) for reference implementation.

describe("GIVEN sidebars object", () => {
  describe("WHEN parsing", () => {
    test("THEN should do something", () => {
      expect(true).toEqual(true);
    });
  });
});
