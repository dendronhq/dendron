import { IDNode } from "../common/types";
import React from "react";
interface NodePreviewProps {
    node: IDNode;
}
export declare class NodePreview extends React.PureComponent<NodePreviewProps> {
    render(): JSX.Element;
}
export {};
